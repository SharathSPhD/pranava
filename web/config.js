// Filled in at deploy time (or edited here) once the Supabase project exists.
// Public anon key is safe to expose (RLS enforces access). Leave blank to run the app in
// endpoint-only mode (no auth; paste a server URL manually — admin/dev fallback).
window.PRANAVA_CONFIG = {
  SUPABASE_URL: "",       // e.g. https://xxxx.supabase.co
  SUPABASE_ANON_KEY: "",  // the project's anon/public key
  ADMIN_EMAIL: "sharath.sathish@gmail.com",
};
